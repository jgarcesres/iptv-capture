{
  "name": "iptv-capture",
  "version": "1.0.0",
  "description": "Captures live HLS streams from Colombian TV channels via headless browser and serves them as M3U playlists",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "playwright": "^1.52.0"
  },
  "engines": {
    "node": ">=20"
  }
}
